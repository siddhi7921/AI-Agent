import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: './',   // <-- FIXED, this solves blank screen and 404
  build: {
    outDir: "dist"
  }
});
